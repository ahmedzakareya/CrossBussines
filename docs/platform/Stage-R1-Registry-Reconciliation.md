# Stage R1 — ADR and SQL Registry Reconciliation

**44 ADRs, all unique. One authored SQL registry, one applied registry. No second competing registry was created.**

---

## 1. ADR registry

| Check | Result |
|---|---|
| Identifiers unique | **44 / 44 unique** |
| ADR-030 … ADR-036 Communication-owned | **Confirmed** — all seven, Third Tab, status `Accepted - RETAINED` |
| ADR-037 Reporting-owned | **Confirmed** — Second Tab, `Accepted - CONFIRMED as the Reporting ADR` |
| ADR-038 / ADR-039 R1-owned | **Confirmed** — Integration Owner |
| Files on disk | 31 documents, `ADR-001`…`ADR-039` with the recorded gaps |
| Numbers available | `ADR-011`, `014`, `015`, `017`–`021` |
| Reserved, not yet written | `ADR-040` Master Data · `ADR-041` ExternalPrincipalContext · `ADR-042` Workspace architecture · `ADR-043` CrossBusiness Blue rollout · `ADR-044` entity registration declarations |

`ADR-038` and `ADR-039` were **written** this increment, moving them from reserved to accepted. Every other reservation stands.

`ADR-012` (Deployment Manifest and Schema History) is recorded as `Accepted - superseded in part by ADR-039`. It is not deleted: it remains the origin of the manifest discipline, and ADR-039 narrows it to the two-registry model rather than replacing it.

**Registry is generated** from the roadmap canonical dataset into `governance/registry/adr-registry.json`, so the document and the enforceable artefact cannot disagree.

## 2. SQL registries — one authored, one applied

| Registry | Role | Location | State |
|---|---|---|---|
| **Authored** | What slices exist, in what order, with what hash and idempotency verdict | `CrossBuy/deploy/sql/manifest.json` | Generated by `scan-sql-manifest.ps1`. **Regenerated this increment.** |
| **Applied** | What ran against *this* database, with what hash, when, by whom, success or failure | `dbo.PlatformSchemaHistory` | Authored; applied and proved on an isolated probe |
| **Claims** | Ownership, canonical-root placement, deployment posture | `governance/registry/sql-slices.json` | Generated; **not** a second registry of slice content |

**No second competing registry was created.** `sql-slices.json` records ownership and posture and points at `manifest.json` as the authored registry of record. The governance validator reads the manifest rather than duplicating it — the applied-side states (`applied` / `changed` / `failed` / `pending` / `missing`) remain solely with `report-schema-history.ps1`.

## 3. The manifest moved during R1 — which is what the snapshot is for

| Measure | R1 design snapshot | Now | Delta |
|---|---|---|---|
| Total scripts | 115 | **123** | +8 |
| `guarded` | 92 | 99 | +7 |
| `no-mutation` | 14 | 15 | +1 |
| `review` | 1 | **1** | — |
| `not-deployable` | 1 | **1** | — |
| `excluded` | 3 | 3 | — |
| `duplicateNames` | 4 | **4** | — |
| … of those, identical | **0** | **0** | — |

Eight new scripts arrived from other tabs' active work. `governance/registry/manifest-facts.json` exists precisely so this cannot happen unnoticed, and it did not: **G6 caught the manifest as stale on the validator's first run**, along with six registry-claimed slices that were absent from it because they had been authored after the last scan. Regenerating cleared both.

## 4. Every active script is represented

| Check | Result |
|---|---|
| Individually-claimed slices present in the manifest | **19 / 19** (G7 clean) |
| Manifest at least as new as every `.sql` file | **Clean** (G6) |
| Duplicate `SliceId` | **None** (G5) |
| New slice authored outside the canonical root | **None** (G4) |

## 5. Divergent duplicates remain explicitly flagged

| Slice | Trees | Distinct hashes |
|---|---|---|
| `pos_hold_recall.sql` | both | 2 |
| `pos_order_guests.sql` | both | 2 |
| `pos_quickmenu.sql` | both | 2 |
| `pos_setup.sql` | both | 2 |

Still four, still **none identical**. The manifest states the consequence itself: *"the same file name in two trees with different content, so `apply <name>.sql` is ambiguous."* G1 blocks on them, and the deployment pipeline refuses to run while they stand.

**Nothing was moved or deleted**, as instructed. Reconciliation requires the POS owner to read both copies (D-39); governance can detect the ambiguity but cannot decide what the schema should be.

## 6. Undeployable scripts still flagged

| Script | Classification | Meaning |
|---|---|---|
| `CrossBuy/deploy/sql/hm16_rename_grni.sql` | `review` | a mutating batch with **no re-run guard that nobody has read** |
| `script.sql` (repository root) | `not-deployable` | *"Unversioned scratch script at the repository root."* |

Both block the pipeline (G2, G3). The manifest generator's own instruction is followed: *"Read it, then add a verdict to `$reviewLedger`. Do NOT widen the guard regex to silence it."* No regex was widened.

## 7. Canonical root

`CrossBuy/deploy/sql` is the canonical **authored** root (D-38, ADR-039). `deploy/` is the deployment **package** root — generated `fresh/`, operational backup and purge scripts, the server README.

57 module slices remain in the package root as a **migration backlog**, listed and not failed. A gate that failed on day one for 57 historical files would be switched off within a week, and a switched-off gate protects nothing.
